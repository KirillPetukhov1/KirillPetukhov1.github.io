
## **[#1] Развертка структуры проекта**

***Цель:***
- Реализация корректной и последовательной структуры проекта
- Наполнение содержимым некоторых файлов. А именно: requirements.txt, .gitignore (общие настройки)
- Продумать хранение файлов (удаление) и их видимость (.gitignore), в том числе их видимость в ветках main и production. Файлы: structure.py, Notes.md, файлы БД с расширением .db
- Реализация подходящих структур файла Отчет.md для веток разработки (main) и итогового продукта (production)

***Итог:***
- Развернута последовательная структура проекта 
- requirement.txt наполнен содержимым
- .gitignore содержит все стандартные скрываемые расширения
- В ветке production удалены structure.py, Notes.md. В ветке main данный файлы присутствуют. Структура файла Отчет.md в ветке production изменена на более лаконичную (в ветке main – рабочая версия)
- файлы БД скрыты

### **Ход работы**

#### **1. Реализация последовательной структуры проекта (main, production)**

На текущий момент дерево директории ветки main (для production есть изменения) выглядит так:

```
./
	README.md
    Notes.md
    Отчет.md
    requirements.txt
    .gitignore
    run.py
    config.py
    structure.py
    
    app/
        routes.py
        __init__.py
        models/
	        __init__.py
        static/
            styles.css
        templates/
            index.html
```
*В папке app можно хранить сами БД, однако они будут скрыты с помощью .gitignore, так в базе  может быть конфиденциальная информация. Данные будут синхронизироваться посредством миграций *

На текущий момент дерево директории ветки production выглядит так (удалены Notes.md, structure.py, изменена структура файла Отчет.md):

```
./
	README.md
    Отчет.md
    requirements.txt
    .gitignore
    run.py
    config.py    
    app/
        routes.py
        __init__.py
        models/
	        __init__.py
        static/
            styles.css
        templates/
            index.html
```

***Объяснение, почему в production удален Notes.md:*** *Notes.md не является файлом, который должен быть виден пользователю* (преподавателю – иначе должен быть удален и Отчет.md), *потому что Notes.md является файлом-заметками*, который помог запустить проект, закрепив "правила игры" и создав первоначальный план развития проекта. В дальнейшем README.md возьмет на себя функцию донесения правил и особенностей проекта, а Issues (и частично Отчет.md) будут отвечать за техническое задание. Конечно, полный план развития проекта был бы удобен, но тут его использование нецелесообразно и излишне, так как в данной ситуации это потеря ограниченного времени. К тому же из-за нехватки знаний и отсутствия надлежащего опыта в сфере фулл-стек веб-разработки возможность составить содержательный, покрывающий все темы план отсутствует (можно реализовать ключевые точки-Milestone, однако в данном проекте их можно держать в голове). ^822bda

#### **2. Формирование отчета в ветках main и production**

В ветке main более полная, удобная рабочая версия. Шаблон:
```
#No. Название задачи 

Цель: Список целей

Итог: Результаты выполнения задачи, краткое описание проделанной работы.

Ход работы: Список выполненных подзадач с описанием

Подзадачи и возможности: Список подзадач (чекбоксы), а также подзадач-дополнений, появившихся в ходе выполнения работы

Вопросы: Список возникших вопросов и идей для обсуждения
```

В production – лаконичная, строго отчетная форма:
```
#No. Название задачи 

Цель: Список целей

Итог: Результаты выполнения задачи, краткое описание проделанной работы.

Ход работы: Список выполненных подзадач с описанием
```

#### **3. Содержимое .gitignore**
Актуальный .gitignore содержится найти в данном репозитории. Это стандартный универсальный .gitignore для python, который в дальнейшем можно не дополнять благодаря его разнообразию и полноте.

В исходный файл было добавлено скрытие файлов баз данных:

```
# Databases
.db
```

#### **4. Содержимое файла requirements.txt**
Первоначальное содержание файла requirements.txt (в дальнейшем возможны правки). Это список используемых в окружении проекта  библиотек с их версиями:

```
﻿alembic==1.17.2
blinker==1.9.0
click==8.3.1
colorama==0.4.6
Flask==3.1.2
Flask-Migrate==4.1.0
Flask-SQLAlchemy==3.1.1
greenlet==3.3.0
itsdangerous==2.2.0
Jinja2==3.1.6
Mako==1.3.10
MarkupSafe==3.0.3
python-dotenv==1.2.1
SQLAlchemy==2.0.45
typing_extensions==4.15.0
Werkzeug==3.1.4
```

### **Подзадачи и возможности**

1. [ ] Сделать другую файловую структуру в production (удалить sctructure.py, Notes.md, поменять Отчет.md)
2. [x] Добавить содержимое .gitignore
3. [x] Добавить содержимое requirements.txt
4. [x] Скрыть .db
5. [ ] Запушить обновленную структуру проекта


### **Вопросы**

0. [x] Фиктивный вопрос (для примера)
**Ответ:** Фиктивный ответ (для примера)

1.  [x] Стоит ли в ветке production в .gitignore добавить Notes.md, structure.py, Отчет.md?
**Ответ:** Notes.md – Да, так как это файл-заметки для первоначального запуска проекта ([[#^822bda|подробное объяснение]]);  structure.py – Да, так как это вспомогательный файл для быстрого и удобного вывода структуры директория; Отчет.md – Нет, это важный файл для итоговой сдачи лабораторной работы (при создании настоящего)
2. [x] Стоит ли выносить Notes.md, Отчет.md в отдельную папку?
**Ответ:** Скорее всего нет. Мы стремимся не усложнять и придерживаться простой (хотя в более крупных проектах – некорректной) структуры проекта, не создавая отдельных папок для уникальных, не масштабируемых файлов.
3. [x] Где хранить файлы БД (.db) и как удобно актуализировать таблицы для работы в команде?
**Ответ:** В папке app. На самом GitHub файлы БД будут скрываться во избежание утечек информации. Синхронизация файлов будет происходить через миграции
4. [x] Что содержит в себе файл requirements.txt?
**Ответ:** Список всех библиотек, используемых в окружении проекта, с версиями. Данные модули очень удобно установить для venv командой `pip install -r requirements.txt` в powershell/cmd.
5. [x] Разобраться с внедрением виртуального окружения (и скрытия его файлов(?))
https://www.reddit.com/r/AskProgramming/comments/qzllsd/gitignore_best_practice/

**Ответ:** Файлы виртуального окружения не пушатся на GitHub за ненадобностью (тяжелые, разворачиваемые только на локальном устройстве файлы). Во избежание ошибок файлы виртуального окружения скрываются с помощью .gitignore.

----
## **[#2] Модель**

***Цель:***
- Спроектировать и реализовать модели данных (User, Expense, Category, PaymentMethod) в соответствии с техническим заданием
- Настроить Flask-SQLAlchemy и Flask-Migrate для работы с базой данных SQLite
- Создать и применить миграции базы данных

***Итог:***
- Получена полнофункциональная модель данных с четырьмя сущностями и связями между ними
- Реализована система миграций для безопасного изменения структуры базы данных
- Настроена интеграция Flask с SQLAlchemy для работы с SQLite


### **Ход работы**

#### **1. Настройка окружения и зависимостей**
Установлены необходимые библиотеки: Flask, Flask-SQLAlchemy, Flask-Migrate, SQLAlchemy. Создана базовая структура проекта Flask с инициализацией приложения. В файле `config.py` настроена конфигурация для SQLite базы данных с указанием пути к файлу БД.


#### **2. Создание моделей данных**
В файле `app/models.py` реализованы четыре модели:
- **User**: модель пользователя с полями id, name, email, is_admin, password. 
- **Expense**: модель расходов с внешними ключами к User, Category и PaymentMethod. Настроены отношения `db.relationship` для удобства доступа к связанным объектам
- **Category**: простая модель категорий расходов с полями id и title
- **PaymentMethod**: модель методов оплаты с полями id и title


#### **3. Инициализация базы данных и миграций**
В основном файле приложения (`app/__init__.py`) проинициализированы расширения: `db = SQLAlchemy()` и `migrate = Migrate()`. Выполнена настройка этих расширений в функции создания приложения.


#### **4. Создание и применение миграций**
Создана начальная миграция командой `flask db init`. Сгенерирован скрипт миграции с учетом всех моделей: `flask db migrate -m "main tables v1.0"`. Миграция применена к базе данных командой `flask db upgrade`. Проверена корректность создания таблиц в SQLite базе данных.


### **Подзадачи и возможности**
- [x] Установить необходимые зависимости (Flask-SQLAlchemy, Flask-Migrate)
- [x] Создать файл конфигурации для базы данных
- [x] Реализовать модели в соответствии с техническим заданием
- [x] Настроить инициализацию базы данных в приложении Flask
- [x] Создать и применить начальную миграцию
- [x] Добавить индексы для часто запрашиваемых полей (user_id)
- [ ] Реализовать валидацию данных на уровне моделей (например, валидация email)
- [ ] Добавить свойства-геттеры для удобного форматирования данных
- [ ] Создать фабрику тестовых данных для разработки
- [ ] Реализовать мягкое удаление (soft delete) для записей


### **Вопросы**
1.  [x] Нужно ли для Flask-Login добавлять в модель **User** свойство `is_authenticated`?
**Ответ:** Достаточно наследовать модель от `UserMixin`


## **[#3] Регистрация и авторизация пользователей**

***Цель:***
- Реализовать регистрацию пользователей
- Реализовать авторизацию пользователей
- Реализовать возможность выхода пользователя из своего аккаунта

***Итог:***
- Реализована регистрация пользователей
- Реализована авторизация пользователей
- Реализована возможность выхода полльзователя из своего аккаунта
- Необходима связь с базами данных, необходимы шаблоны для форм регистрации и авторизации

### **Ход работы**

#### **1. Реализация регистрации пользователей**

Для регистрации пользователь должен зайти на /register и заполнить форму. После заполнения формы данные обрабатываются и передаются в обработку базам данных и если создается новый пользователь, добавляется новая запись, содержащая имя пользователя, почту, пароль. Данные полученны из формы. После заполнения формы, в случае успешного заполнения, пользователь переадресуется на форму авторизации.

#### **2. Реализация авторизации пользователей**
Для регистрации пользователь должен зайти на /login и заполнить форму. После заполнения формы данные обрабатываются. Из базы данных достается запись пользователя с именем, введенным в форму. Затем сверяется параль, полученный из формы и находящийся в базе данных. Если пользователь верно указывает данные, то он входит в сессию и перенаправляется на главную страницу (или на другую страницу, с которой был переадресован, на данную форму). Если пользователь до этого входил, при попытки зайти на /login, он будет сразу переадресован в свой личный кабинет.


#### **3. Реализация возможности выхода пользователя из своего аккаунта**
При переходе пользователя на страницу /logout, пользователь выйдет из своего аккаунта и перенаправиться на страницу /login


### **Подзадачи и возможности**

1. [ ] Сделать в формах регистрации всплывающие сообщение, отображающие некоторую информацию
2. [ ] Подключить базы данных и реализовать функции db.addUser, db.getUserByName



## **[#4] Создания страниц для администраторов сайта** 

***Цель:***
- Реализовать сраницы, доступные только администраторам
- Реализовать страницу /admin/people.
- Реализовать страницы пользователей, доступные только администраторам по /profile/<id>

***Итог:***
- Реализованы сраницы, доступные только администраторам
- Реализована страница /admin/people.
- Реализованы страницы пользователей, доступные только администраторам по /profile/<id>

### **Ход работы**

#### **1. Реализация сраниц, доступные только администраторам**

Для реализации страниц используются 3 функции: 
1) Проверка, что пользователеь авторизирован через @login_required 
2) current_user.get_id() для получения ID текущего пользователя
3) db.getUserByID получение информации о пользователе из баз данных
Далее идет проверка, что пользователь является администратором (если не является, будет перенаправлен на главную страницу) и получает доступ к административным страницам сайта.

#### **2. Реализация страницы /admin/people**
Происходит проверка пользователя, если он администратор, ему дается доступ к данной странице. Далее создается массив из словарей, где для каждого словаря указано некое имя (Пользователь (?)) и url личного кабинета данного пользователя. Данный список передается в шаблон и на странице отображаются ссылки на страницы пользователей, доступные только администрации.


#### **3. Реализация страниц пользователей, доступные только администраторам по /profile/<id>**
Происходит проверка пользователя, если он администратор, ему дается доступ к данной странице. Каждая страница получает данные из базы данных о конкректном пользователе. Данные страницы будут идентичны обычным страницам пользователей.
**Ответ:** 


## **[#5] CRUD'ы, формы и маршруты**

***Цель:***
- Разработать полнофункциональные CRUD-контроллеры для основных моделей приложения (**User**, **Expense**)
- Реализовать систему прав доступа с разделением на обычных пользователей и администраторов
- Создать формы валидации данных и систему фильтрации для расходов
- Организовать структуру приложения по модулям для поддержки командной разработки

***Итог:***
- Создана полная архитектура CRUD-операций для 2 моделей с использованием Flask-SQLAlchemy
- Реализована система аутентификации и авторизации с помощью Flask-Login
- Разработана валидация форм через Flask-WTF с кастомными валидаторами
- Внедрена система прав доступа с декораторами для разделения функционала между пользователями, администраторами и гостями
- Создана пагинация и фильтрация данных с поддержкой статистики
- Подготовлена модульная структура для легкой поддержки и расширения приложения


### **Ход работы**

#### **1. Создание форм валидации**
Разработаны 5 классов форм с использованием Flask-WTF. Реализованы кастомные валидаторы для проверки уникальности email, названий категорий и методов оплаты.


#### **2. Реализация системы прав доступа**
Созданы декораторы для контроля доступа:
- `admin_required` - только для администраторов=
- `user_or_admin_required` - для владельца ресурса или администратора


#### **3. Разработка CRUD-контроллеров**
Создан полный набор контроллеров для моделей **User** и **Expense**. Реализованы пагинация, фильтрация по датам, категориям и методам оплаты, сортировка по дате и сумме


## **[#6] Аутентификация**

***Цель:***
- Реализация функциональности регистрации и входа пользователей
- Создание интуитивно понятного пользовательского интерфейса для аутентификации
- Интеграция WTForms с Bootstrap для стилизации форм
- Обеспечение безопасности и валидации данных на стороне сервера

***Итог:***
- Реализованы маршруты /login и /register с обработкой GET и POST запросов
- Созданы формы LoginForm и UserCreateForm с полной валидацией данных
- Разработаны HTML-шаблоны login.html и register.html с использованием Bootstrap 5
- Реализована система флэш-сообщений для обратной связи с пользователем


### **Ход работы**

#### **1. Разработка форм аутентификации**
Созданы две формы WTForms: `LoginForm` для входа и `UserCreateForm` для регистрации. В `LoginForm` реализованы поля для email, пароля и опции "запомнить меня". `UserCreateForm` включает поля для имени, email, пароля и подтверждения пароля с валидацией на минимальную длину (6 символов) и совпадение паролей. Добавлена кастомная валидация email для предотвращения дублирования пользователей в базе данных.

#### **2. Создание маршрутов Flask**
Разработаны два основных маршрута:
- `/login` - обрабатывает вход существующих пользователей. При успешной аутентификации перенаправляет на главную страницу или запрошенный ранее URL. Включает проверку на уже аутентифицированного пользователя.
- `/register` - обрабатывает регистрацию новых пользователей. После успешной регистрации перенаправляет на страницу входа с соответствующим сообщением.

#### **3. Разработка HTML-шаблонов**
Созданы шаблоны с использованием Bootstrap 5:
- `login.html` - компактная форма входа с ссылкой на регистрацию
- `register.html` - двухколоночная форма регистрации с валидацией
- `auth-base.html` - базовый шаблон с подключением Bootstrap
Шаблоны используют системные сообщения Flask для отображения ошибок и успешных операций, а также интегрированы с WTForms для автоматического рендеринга полей с валидацией.

#### **4. Реализация функциональности безопасности**
Внедрены следующие механизмы безопасности: хеширование паролей через check_password_hash, защита от CSRF через скрытое поле WTForms и валидация входных данных на стороне сервера


### **Подзадачи и возможности**
- [x] Создание форм WTForms для входа и регистрации
- [x] Реализация маршрутов Flask с обработкой GET/POST запросов
- [x] Разработка HTML-шаблонов с Bootstrap
- [x] Интеграция флэш-сообщений для обратной связи
- [ ] Добавление подтверждения email (потенциальное улучшение)
- [ ] Реализация восстановления пароля
- [ ] Добавление капчи для защиты от ботов
- [ ] Интеграция OAuth-авторизации (Google, GitHub)
- [ ] Реализация двухфакторной аутентификации


### **Вопросы**
1.  [ ] Стоит ли добавлять проверку сложности пароля?
**Ответ:**

----
## **[#7] Создание шаблонов HTML**

**Цель:**
- Создание интерфейса сайта (фронтенд разработка)
- Семантическая верстка
- Добавление скриптов для графика, круговой диаграммы (страница [[#Статистика|статистики]]) и возможности переключения режима редактирования-просмотра данных в личном кабинете (страница [[#Личный кабинет|личного кабинета]]), появления модального окна подтверждения Удаления аккаунта (страница [[#Личный кабинет|личного кабинета]])
- Создание первоначальных возможностей взаимодействия (кнопки, формы), связей с БД (заготовки кода) посредством контроллера (Flask + Jinja)

**Итог:**
- Сверстаны 4 страницы: главная, расходы пользователя, статистика по расходам, личный кабинет
- Созданы скрипты: `Chart.js` для отображения графика динамики расходов пользователя, диаграммы "пончик" соотношения расходов по категориям расходов (страница [[#Статистика|статистики]]); toggle для переключения режима просмотра-редактирования данных в личном кабинете (страница [[#Личный кабинет|личного кабинета]]); скрипт появления модального окна подтверждения удаления аккаунта (страница [[#Личный кабинет|личного кабинета]])

### **Ход работы**

#### 1. Главная страница

**Структура:**
```
index.html

Шапка (это все отдельные страницы, то есть ссылки). 
Мои расходы
Статистика
Личный кабинет (если сессия не найдена - кнопка зарегистироваться/войти - ведет в форму – тег form)

Основное содержание.

Подвал.
Список авторов
Копирайт 2025
ITMO
```

**Функционал:**
- Переключение между страницами в хедере (все страницы будут иметь возможность переключения посредством списка ссылок в шапке)

^76fb2f

#### Расходы
Эта страница доступна по ссылке [[#^76fb2f|Мои расходы]] в шапке главной страницы.

Структура:
```
expenses.html:

Шапка.
Мои расходы (выбрано)
Статистика
Личный кабинет

Основное содержание.
табличка (смотри листочек):
th (?) кнопка добавить (может значок плюс) (элемент CRUD), список кнопок фильтр, сортировка (управление через контроллер)
th (?) Категория, трата, комментарий, сумма (можно оформить через span)
td ... + изменить, удалить (элементы CRUD)
td ...

Подвал.
Список авторов
Копирайт 2025
ITMO

```

**Функционал:**
- Заготовка возможности CRUD (добавления, удаления, изменения) информации о расходах
- Заготовка панели управления расходами: фильтр по категории, сортировка по дате, сортировка по сумме)

#### Статистика
Эта страница доступна по ссылке [[#^76fb2f|Статистика]] в шапке главной страницы.

**Структура:**
```
statistics.html:

Шапка.
Мои расходы 
Статистика (выбрано)
Личный кабинет

Основное содержание.
Фильтр. 
Элементы статистики (можно добавить к тому же и графики), включая диаграмму "диск" по расходам (как в т-банке)

Подвал.
Список авторов
Копирайт 2025
ITMO

```

**Функционал:**
^1ddf8b
- Панель фильтрации статистики. Период: месяц, квартал, год, все время. Категория: одна, все. Диапазон дат. 
- Заготовки блока общей информации (связь с БД). Всего потрачено, средний расход, самая частая категория трат, кол-во записей, распределение по категориям (диаграмма "пончик"), график динамики расходов (сумма расходов за каждую запись).


#### Личный кабинет
Эта страница доступна по ссылке [[#^76fb2f|Личный кабинет]] в шапке главной страницы.

**Структура:**
```
account.html:

Шапка.
Мои расходы 
Статистика
Личный кабинет (выбрано)

Основное содержание (тут у нас только административная информация про пользователя).
Имя (все персональные данные будут анонимизированны в БД?)
email 
Зарегестрирован на платформе с "дата"
Пароль (рядом поменять пароль, забыли пароль?)

Удалить аккаунт - вы уверены, что хотите удалить аккаунт (для тестов БД убрать "вы уверены")

Можно переключить в режим изменения (toggle) - скрытые элементы появляются (.hidden)
В каждом поле появляется возможность изменить любое поле (элемент CRUD)

Подвал.
Список авторов
Копирайт 2025
ITMO

```

**Функционал:**
- Возможность переключения между режимами просмотра данных о пользователе и редактированием этих данных (JS)
- Появление модального окна подтверждения удаления аккаунта. Заготовка функции удаления аккаунта
- Формы изменения информации о пользователе. Имя пользователя, email, изменение пароля
- Заготовка формы "Забыли пароль?"


### **Возможности и подзадачи**

- [ ] переделать блок фильтров/сортировки в expenses.html (подумать надо ли) – привести к однообразному формату (как в statistics.html)
- [ ] добавить возможность фильтрации по нескольким категориям, а не одна/все (statistics.html)
- [ ] разобраться за какой промежуток времени показана информация в графике динамики расходов

### **Вопросы**


----

## **[#6] Аутентификация**

***Цель:***
- Создать полнофункциональную админ-панель для управления пользователями и просмотра статистики расходов
- Реализовать систему фильтрации и визуализации данных для администратора
- Обеспечить удобный интерфейс для анализа расходов пользователей

***Итог:***
- Создано 5 полноценных шаблонов HTML с использованием Bootstrap 5
- Интегрирована система фильтрации через WTForms с поддержкой валидации и динамическими выборками
- Добавлена визуализация статистики через Chart.js (круговые диаграммы и линейные графики)
- Реализована пагинация списка пользователей с навигацией по страницам


### **Ход работы**

#### **1. Проектирование структуры админ-панели**
Определены ключевые разделы админ-панели: управление пользователями и общая статистика. Спроектирована навигация с активным выделением текущего раздела. Создан базовый шаблон base.html, содержащий общие элементы (шапку, меню, футер) и стили для всех страниц. Реализована система декораторов @login_required и @admin_required для защиты маршрутов.

#### **2. Разработка системы управления пользователями**
Создан маршрут /admin/users-list с пагинацией, отображающий список пользователей в виде карточек. Каждая карточка содержит основную информацию о пользователе и кнопки для перехода к детальной информации о его расходах и статистике. Реализована постраничная навигация с использованием paginate() из SQLAlchemy.

#### **3. Создание страницы просмотра расходов пользователя**
Разработан маршрут /admin/user/<int:user_id>/expenses с формой фильтрации ExpenseFilterForm. Форма включает фильтры по дате, категории, способу оплаты и параметрам сортировки. Результаты отображаются в таблице с возможностью сортировки по разным критериям. Для формы использован макрос render_form из Flask-Bootstrap.

#### **4. Реализация системы статистики**
Созданы два маршрута для статистики: user_statistics (для конкретного пользователя) и total_statistics (общая статистика). Разработана форма StatisticFilterForm для фильтрации данных по датам, категориям и способам оплаты. Реализованы функции для расчета ключевых метрик: общая сумма расходов, средний расход, самая частая категория, количество записей.

#### **5. Интеграция визуализации данных**
Добавлены интерактивные диаграммы с использованием Chart.js: круговая диаграмма для распределения расходов по категориям и линейный график для отображения динамики расходов по времени. Созданы вспомогательные функции get_category_data() и get_time_data() для подготовки данных в формате, подходящем для Chart.js.


### **Подзадачи и возможности**
- [x] Создать базовый шаблон с навигацией
- [x] Реализовать список пользователей с пагинацией
- [x] Разработать страницу расходов с фильтрацией
- [x] Создать систему статистики с графиками
- [ ] Создать систему логов действий администратора
- [ ] Добавить поиск пользователей по имени/email
- [ ] Добавить дашборд с ключевыми метриками на главной админ-панели


### **Вопросы**
1.  [ ] Стоит ли добавить систему ролей с разными уровнями доступа?
**Ответ:**